# Crystocraft Operation Center

This repo's agent brief lives in **[`CLAUDE.md`](CLAUDE.md)** — read that first.
It is the canonical, maintained entry point (orientation + the `docs/skills/`
system it points to). This file is only a signpost so the `AGENTS.md`
convention resolves; do not duplicate content here.
